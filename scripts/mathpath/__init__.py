"""Generator for the Discrete Mathematics path.

The trading path arrived as eight authored HTML packages and was normalized
INTO the library's conventions by scripts/intake_course.py. That direction --
foreign package in, conventions imposed afterwards -- is what produced every
drift TestPinnedConventions now pins: three pager families, eight theme keys,
five light palettes.

This path is authored the other way round. The content is data
(content/discrete_math/), the chrome is one renderer (chrome.py), and the
interactive parts are a fixed kit of exact widgets (labs.py). A convention
cannot drift here because there is exactly one place it is written.
"""
